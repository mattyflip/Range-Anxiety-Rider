rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // TEMPORARY: Open database for debugging
    // WARNING: This makes your data public. We will revert this immediately after testing.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
